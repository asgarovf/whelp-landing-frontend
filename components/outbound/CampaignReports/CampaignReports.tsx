import BackgroundImage from 'assets/images/outbound/campaign-reports/right.png';
import { useGap } from 'hooks';
import Image from 'next/image';
import { Content, Header, Text } from 'ui';

import { DualGrid } from 'components/DualGrid/DualGrid';

import styles from './CampaignReports.module.scss';

export const CampaignReports = () => {
  const gap = useGap({
    small: 40,
    medium: 40,
    large: 24,
  });

  return (
    <DualGrid
      paddingY={80}
      leftClassName={styles.left}
      gap={gap}
      left={<Left />}
      right={<Right />}
    />
  );
};

const Left = () => {
  return (
    <Content>
      <Header inheritStyles="h1" as="h2">
        Keep track of your campaigns with detailed reports
      </Header>
      <Text className={styles.subtext} subtext>
        Monitor the performance of your campaigns with detailed reports,
        providing valuable insights for future planning.
      </Text>
    </Content>
  );
};

const Right = () => {
  return (
    <div className={styles.imageWrapper}>
      <div className={styles.inner}>
        <Image
          className={styles.imageBg}
          src={BackgroundImage}
          alt="CampaignReports background"
        />
      </div>
    </div>
  );
};
